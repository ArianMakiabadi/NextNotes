import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Products',
  description: 'Browse all products in the catalog.',
};

function Page() {
  return <div>here are the products</div>;
}
export default Page;
